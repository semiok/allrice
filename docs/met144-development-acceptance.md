# MET-144 X01-C：真实开发协作验收

状态：进行中。2026-09-22 已补齐后台配置 / 就绪 / 证据展示，并完成一次真实 Snow → M5 文件读取；完整协作任务未通过，不关闭 MET-144。下方 2026-09-21 的准备记录是历史，当前状态以本节为准。

## 2026-09-22：后台补齐、M5 实测与恢复

### 已交付到 Dev 的增量

- 固定运行候选 `b463de6b792345b63aabfee44947e13c1fb44bf5`，发布目录 `met144-admin-fix-20260922`。仅 Dev；main / Prod 未改、MET-145 / MET-146 未改。此前中间候选为 `3d11016e`、`febb123f`。
- 员工生产 → 工具：新增开发协作缺项说明及“补齐开发协作工具（仅修改草稿）”。按钮不自动保存、发布、批准策略或授权本地目录；运行包仍须真实试用及指定租户发布。最小依赖同时在编译 / 发布端校验。
- 租户管理 → Snow → 能力 / 验收：开发协作单独显示可用性。核对已发布员工工具、策略、运行开关、在线设备及 `changeset_candidate` Runner 能力；一般命令 Runner 在线不等于能测试候选版本。委派策略需要明确允许，命令和写回仍逐次审批。
- 租户 Run 检查：只读展示候选 SHA、提案作者、同版本命令回执、独立审查与交付关联。跨租户 / 跨使用者请求被拒；结果不匹配或只有模型声明时不显示已验证，旧版本通过不能证明当前候选通过。无原始凭证、命令环境或未脱敏输出扩散。

### 真实验收事实（不是完整通过）

1. 通过真实管理网页保存 Snow 临时策略和员工草稿；真实 Codex 试用 `8918d3fd-ca84-4ce3-8da0-bcd6e62d5a5f` 成功，只发布给 Snow。实际使用者仍是普通成员；后台 / 租户 readiness 均观察到开发协作可用。
2. M5 ARM 候选 Core SHA `74fdeb7279c59b1af3c72a45c235e7949a166e1ab1886b37678d6f25de82c0fe`，独立 App，保留原 App / 配对 / 目录授权。原 App 空闲退出曾超时；核对服务器无在途任务、本地日志均已交付后才结束该旧进程。候选 App 本轮正常退出并恢复原 App，不能据此说旧包退出问题已修好。
3. 在已授权目录下新建独立 `met144-development-20260922` 两文件 Node 项目，无依赖安装、无外网请求。真实 Snow 网页提交 Run `e13f007a-1675-4fe8-9838-5fff8f23527b`；M5 返回两个文件的正文和 SHA。基线 Changeset 已发布，但协作初始化参数连续失败，未生成开发主线、未委派三个助手、未提交命令 / 批准 / M5 测试，不能宣称端到端通过。
4. 实测发现并修复：导出工具没有返回后续协作必需的工件 digest；无效 seed 只收到泛化错误。现在返回持久化规范化对象的真实 checksum，并提供完整 initialize 示例和可纠正的有界错误。无效参数不执行开发动作，不回显原始输入或绕过文件 / 身份校验。另修正命令说明中“子助手尚未接通”的过时文字。修复版已部署，但尚未重新完成真实模型链路。
5. 失败不能归因于 Google、M5 离线或官方订阅耗尽。首轮错误为 `DSH_UNKNOWN`，自动重试被 `MODEL_TOKEN_USAGE_UNKNOWN` 拦截。进一步只读核对：前 6 次模型 admission 已完成；第 7 次只有 prepare、`dispatched_at = null`，其预留未结算；根账本已记录 input 47,002 / output 3,214，但 route 失败聚合记录仍为 incomplete。未把 prepare 量充当真实消耗，未删除历史、清零预算或新建任务绕过。需要继续修复 / 核对派发前失败分类及预留结算，再重跑；管理员预算审查也不能冒充实际用量回执。本轮没有执行新的预算例外处理。
6. 用真实后台 Run 检查该失败任务：显示没有开发验证记录，而不是假“测试通过”。保存私有截图与 API 证据。

### 已完成的验证与恢复

- 后台 / 契约 / PostgreSQL / 浏览器定向 7 文件 66 项通过；命令可见性 6 项通过。上述缺口修复的 Tool Broker / 原生参数单测 26 项通过；生产 DSH + 隔离 PostgreSQL 的原生编排 2 项通过（合成模型和设备，不冒充 M5 证据）。各组有不同边界，不与旧全仓数字累加。
- Worker、Web、Database 类型检查、集成 TypeScript、变更 ESLint、格式和差异检查通过；最终候选全工作区构建通过。Dev Web / Worker、本地和两个公网入口 readiness 200，Web release SHA 与候选一致。
- 通过正式网页恢复原员工定义，再真实试用 `eb23ba4a-0560-47b6-83ed-727eabcc463d` 成功并仅发布 Snow，保留新版本记录。首次恢复发布与策略恢复并发，快照门禁返回 409；确认没有发布后重新预检才成功，没有绕过冲突。策略恢复到原停用 / plan-only 语义；临时 5 个服务开关恢复。确认角色、500 万额度、Prod 配置未变，服务器无在途任务。
- M5 已恢复原 App、原目录授权、原沙箱关闭设置。两个测试文件 SHA 不变；为可恢复而移到独立下载验收目录归档，未删除用户项目、配对或历史。

### 尚未通过，仍需继续

先处理上述未派发预留 / 用量聚合阻断，再用修复后的固定候选完成“网页任务 → 编辑提案 → 同版本网页审批 / M5 测试 → 独立审查 → 主 Rice 交付”。拒绝 / 修正重测、用户编辑、离线 / 刷新、取消恢复按下方清单补证。不能以后台配置完成、成功试用或已有合成回归关闭工单；不因验收卡住扩大默认预算或启用 145 / 146。

## 2026-09-21：授权与固定版本（历史准备）

- 用户明确批准仅 Dev / Snow / M5：可部署候选及新增元数据迁移，通过正式管理页面临时配置 Snow 的策略、员工工具，并在 M5 独立测试目录运行候选 Bridge / 沙箱。结束后恢复临时配置。
- 不合并 main、不动 Prod、不动 MET-145 / MET-146；Snow 保持普通成员及长期 500 万内部月 Token 额度。保留设备配对、原目录、真实用量和失败记录。
- Web / Worker / Bridge 源码候选：`b7e96461de4d890249c4ec3fcc5ac31bf0bf4d22`，对应 PR #85。未合并依赖仍为 #82 → #83 → #84 → #85；本次部署不代表这些 PR 已合并。
- Dev 原版本：`b787bd95c2d3c182aee39070f7daf7e00846aae3`。Prod 仍为 `5d246ba7a85b5640121cbcc8dc1336f52d0a74df`，未重启或替换。

## 已完成的准备与验证

1. 部署前无在途任务。保存原 Dev 启动配置、环境文件摘要、发布指针、Snow 策略 / 员工版本 / 角色 / 额度、Prod 进程及配置摘要；独立备份 Dev PostgreSQL，权限 0600。数据库备份不冒充对象存储或设备文件备份。
2. 固定候选与兼容回退版分别完成依赖安装、Web / Worker / Bridge 构建。沿用固定候选已通过的四项 CI，不重复全仓测试凑次数。
3. 停止 Dev 接收新任务并核对排空后，应用 `0101_development_cooperation.sql` 和 `0102_development_verification.sql`。只新增协作元数据，未更改额度、员工发布配置或授权。
4. 回退版以原 Dev 源码为基线，仅增加相同的 0101 / 0102 迁移文件，避免旧二进制因迁移清单不一致而无法启动。候选和回退版均执行 `db:verify`；回退版实际启动并通过健康检查，再切换候选。新开发任务一旦开始，回退仍须先停止新建、排空 / 确认停止，不能让旧执行逻辑接管在途开发任务。
5. Dev Web / Worker 本地 readiness 和 Snow / DSH 公网 readiness 均返回 200，公网 release SHA 与固定候选一致。部署后的权限、员工版本、月额度与原值一致；登录引起的 assignment `updated_at` 刷新不被误当作版本或权限变更。
6. 通过实际后台登录和员工工具页面确认 `assistant.development` 已注册、入口可见且未勾选；没有保存草稿、发布员工版本或扩大授权。Snow 原策略仍停用，高权限执行开关保持原值。Snow 实际网页登录、历史与三栏工作台检查通过，页面脚本异常为零；月额度 API 显示限额 5,000,000、已用 2,950,278、剩余 2,049,722（40.99444%，界面向下取整为 40%），身份仍为普通成员。查看历史 Gemini 对话不等于调用 Gemini，本轮没有提交任何模型任务。
7. 已构建独立 ARM 候选 App，Node 22.23.2 的 ARM / Intel 构建输入逐个对照官方归档 SHA-256，并校验已解压 Node 二进制；App ad-hoc 签名校验通过。未 Apple 公证、未发布前台下载、未替换 M5 原 App、未启用 M5 沙箱。该包必须结合源码 SHA 和包摘要识别，不凭沿用的开发版版本号声称设备已更新。
8. 使用现有平台绑定、官方原生 Codex app-server 只读额度接口，观察到周窗口已用 85%、未耗尽；没有模型生成调用，不将其换算为 AllRice 内部月 Token 余额。初次探测使用 npm 的 Node 包装入口，在受限 PATH 中启动失败；切换同一已安装版本的原生二进制后成功，没有修改服务配置或放宽进程环境。

验收脚本、数据库备份、精确设备 / 用户标识、屏幕截图和发布配置只保存在本机私有证据目录，不提交凭证、会话 Cookie 或数据库导出。页面等待应以元素就绪为准，不把 SSE / 轮询页面的 `networkidle` 超时当成应用失败；同时保留实际公网 TLS 失败记录，不将传输失败报告为通过。

## 待完成：真实 M5 项目链路

2026-09-21 的阻挡条件是 M5 网络不可达；2026-09-22 已恢复并按上节实测。以下为未完成链路的验收清单，不是通过声明。继续沿用已批准的 Dev / Snow / M5 范围；每次测试仍先核对实际设备及配置。

按下列顺序推进，不跳过审批，也不重新做已在 A / B1 / B2 覆盖且没有变更的全量回归：

1. 核对 M5 原配对、获准目录、沙箱配置和在途任务；保存原 App 身份。独立测试目录只放本轮新建的小型 Node 项目，无外部依赖或网络安装，不操作用户现有项目。
2. 正常退出旧 Bridge，使用候选 App，并核对实际 `changeset_candidate` 能力和 ARM Runner。保留原应用；启用已有独立沙箱不等于授予任意宿主 Shell。
3. 通过后台仅配置 Snow：所需本地读取 / 助手策略、逐次命令 / Changeset 审批、`assistant.development` 等员工工具。用真实试用与 Snow 单租户发布记录证明配置生效，不修改数据库伪造发布或升管理员。暂时开启的服务端开关不得使其他租户越过自身策略与员工权限。
4. 由 Snow 新建 **Codex 订阅**任务，不沿用历史 Gemini Session 的路由。用户从正常网页入口提出小项目修改；主 Rice 初始化候选，编辑助手提交提案，主 Rice 合并，测试助手提议针对准确候选的命令。
5. 从网页核对 Diff / 命令 / 输入版本并精确批准；M5 在隔离副本执行。独立审查助手读取同版代码及真实回执，主 Rice 保存正式交付工件。记录根 / 子 Run、候选 SHA、操作 / 批准 / 回执、审查及交付引用。检查原目录未被候选测试提前修改；真正写回是另一项 Changeset 批准。
6. 补新增链路的拒绝 / 修正 / 同版本重测、用户中途编辑、离线 / 刷新恢复及取消 / 迟到结果等必要实测，逐项标明物理设备、真实模型与已有隔离夹具的证据边界。不得把旧版本的批准 / 测试迁移到新版本，不以新 Run 清零失败任务预算或重放未知写入。
7. 恢复临时策略、员工发布配置、开关、Bridge / 沙箱状态和本轮测试文件；保留原文件、原配对、Snow 普通成员身份、500 万月额度、真实用量与失败记录。核对其他租户和 Prod 未受影响。

若实际任务暴露缺陷，在 X01-C 独立分支修复并针对变更重新验证；不能用本轮构建、后台工具可见或旧合成 VM 测试代替上述真实交付。X01-C PR 和关单仍以实际证据为准。
