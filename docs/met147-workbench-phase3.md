# MET-147 · UX01-C：真实租户验收与恢复修复

独立 PR，叠加 UX01-B（PR #74）；不合并 main，不部署 Prod。用户授权仅 Dev 的 Snow 灰度。本文中的自动化层级分别标注，隔离组件/协议测试不算真实 Codex 任务通过。

## 本次修复

- **历史会话刷新恢复**：选择会话时以 `replaceState` 记录地址中的 `session`；新工作清除旧定位，迟到请求不改写选择。刷新仍须经工作区作用域鉴权，不信任 URL 作为授权。保留 Next 路由状态及其他查询参数，清除旧会话审批锚点；History 写入不可用时不中断任务。
- **分页外的旧会话**：侧栏最近 30 条不是权限索引。URL 指向更旧会话时，调用既有按工作区鉴权的 history 接口，成功后才加入本地列表；403/404 不泄露工件，临时网络错误不静默跳到别的任务。员工灰度发布批量更新时间曾让真实研究会话掉出首页，由复验捕获。
- **抽屉关闭焦点**：宽屏内编辑后切窄屏，不再把即将卸载的内部输入框当返回目标；保留外部入口，且不覆盖父组件指定的焦点。
- **候选版本辨识**：ready 健康接口在配置合法的 `ALLRICE_RELEASE_SHA` 时提供 `X-AllRice-Release-Sha`，禁止缓存。不猜测版本；验收脚本在登录/发送前核对部署 SHA。
- **聊天交付入口**：真实任务中模型把相对下载路径拼成了 `allrice.example`。只对当前 Run 已鉴权工件清单中的精确文件 ID，将标准下载路径规范化为站内地址和真实文件名；不改写未知文件或普通引用，不修改已保存消息，不绕过下载鉴权。
- **模型 Changeset 提案接线**：原导出工具仅接受 document/plan，所谓“专用 Changeset 入口”未接到主 Rice。现在以 `artifactKind=changeset` 接通既有发布器：模型只提交相对路径及 before/after 文本，服务端解析当前用户的在线 Bridge/目录并计算 SHA；严格拒绝模型指定执行绑定。输出明确为提案、未执行，仍须请求应用、精确审批、实际基线校验。没有增加第二套写文件通道或改变旧 Run。
- **浏览器权限检查锁顺序**：真实网页审批后触发 Job→Run 与多表 `FOR SHARE` 中 Run→Job 的死锁。先按同用户/工作区取得 Job 共享锁，再做完整 lease/authority 校验，不取消任何授权条件。真实 PG 并发测试在去掉修复时失败、恢复修复后通过，不靠重放浏览器操作解决死锁。

## 可重复的真实网页验收

入口：`scripts/acceptance/ui/met147-tenant-workbench.mjs`。使用独立 Chromium、实际租户登录、真实 HTTP/数据库/Worker；不注入模型返回、不修改历史 Run、不读取个人浏览器配置。

由私有启动脚本提供环境（不要把密码放入命令历史、PR 或验收记录）：

- `ALLRICE_ACCEPTANCE_BASE_URL`：明确 Dev 地址。
- `ALLRICE_ACCEPTANCE_SHA`：完整候选 SHA，与在线健康响应一致。
- `ALLRICE_ACCEPTANCE_WORKSPACE_ID`：被授权的工作区。
- `ALLRICE_ACCEPTANCE_USER` / `ALLRICE_ACCEPTANCE_PASSWORD`：既有租户成员登录。
- `ALLRICE_ACCEPTANCE_EVIDENCE`：私有证据目录；截图可能含租户内容，不提交仓库。
- 可选 `ALLRICE_TEST_CHROME_EXECUTABLE`：隔离浏览器可执行文件。

模式：

1. 默认 `--inspect`：不发模型任务；验证历史会话刷新、成员的十二项能力入口、窄屏及 JS 错误。仅登录/登出产生身份会话写入。
2. `--submit-research`：**明确授权才使用**，从网页发送一条 COIN/MSTR/CRCL 研究任务。先独占写发送意图，防止响应丢失后自动重发。保存实际 Session/Run，最多观察 15 分钟；超时不擅自取消或再建任务。
3. `--resume`：只观察同一证据目录的既有 Run，绝不重新发送。跨代码版本保留最初提交 SHA，区分原冻结执行与当前前端。

研究验收检查：成功终态、来自该 Run 的真实 document Artifact、所有者/会话一致、三家公司与引用、右栏自动选中、表格渲染、真实下载与 SHA 校验、刷新恢复及无正常完成预算警告。报告内容和来源质量还须人工核对；存在三个 URL 本身不是事实正确性的证明。

## 灰度与待验收边界

- 首次 Dev 候选 `56d00b6` 保留了原有开关，无新授权；Prod 进程及配置哈希未变。
- 真实网页首次提交 Run `f2f910be-fd80-4cb7-a433-dde4e22a5d87` 被 `MODEL_TOKEN_QUOTA_EXCEEDED` 拦截，未进入模型。Snow 真实本月累计 2,213,797 Token 已超过默认用户月限额 2,000,000；不能说成官方 Codex 周额度耗尽。
- 用户另行批准：仅 Snow 的用户月限额临时 5,000,000，验收结束恢复。真实账本、异常预留、组织/员工限制及单 Run 规则保持不变。此操作不构成生产默认预算调整，预算长期取值继续归 MET-150。
- 已通过 SSH 检查 M5：运行 `0.5.0-dev.1`，固定 ARM Node 沙箱可用；未重配、未替换原 AI-what 目录、未开启命令权限。当前配对逻辑每工作区替换已有设备，不通过重配 Intel 撤销 M5。
- 完整关单仍要求真实研究报告、网页审批→Bridge 文件/命令→交付、真实浏览器证据及异常恢复。只完成上述修复/脚本不得把整个 UX01-C 或 MET-147 标 Done。

## 2026-09-21 当前证据（分层，不累加重叠用例）

- 草稿 [PR #75](https://github.com/semiok/allrice/pull/75)，基于 #74；Dev 首次完整构建候选 `a7c41f1b6728f3d39e496dc273407c967640b428`，不合并、不动 Prod。最终修复后的部署 SHA 与复验结果补在 PR/Linear，不能用旧 SHA 冒充。
- **真实 Snow / Codex 订阅**：网页提交 Run `b37598ef-46be-40b2-a715-9d33d6a9c008`、Session `16d9434a-7649-47b6-9c7a-ce0e31d991ba`。多步检索后 succeeded；报告自动展示、表格、真实下载 SHA 和刷新恢复均通过，无正常完成预算误警告。输入 236,719，输出 7,477，总量 244,196，其中缓存输入 200,192（已包含在输入中，不再相加）。
- 工件 `5d50a0a8-9e56-4f27-a599-8f6c2da3da40`，对象 `8e11d67c-6718-42c9-99f2-de4530771fdd`，6,885 字节；SHA-256 `b42430c0b1688ad83659b9544cec18acc6315981fa4281cfdea0ae0855434586`。首轮脚本未检查聊天摘要的链接，人工检查发现假域名后补修复与断言；必须复验同一个 Run，不能只引用首轮通过。
- **真实租户只读网页验收**：成员身份下的十二项入口、宽窄屏、历史 Session 选择/刷新通过；无 JS 错误。没有把成员提升为管理员。
- **隔离数据库 + 真实 HTTP/VM/Chrome**：governed Bridge / 工作台 / 输入隔离 112 项通过。新增聊天链接回归验证：只规范化本 Run 的已鉴权工件，其他 Run 的相同文件 ID 也不能借此解析。
- **隔离浏览器控制链**：自有临时 HTTPS 合成站点，真实私有 Chrome/HTTP/PG/固定云 Chromium；网页授权、刷新恢复、敏感输入、实际登录/上传/下载、暂停/恢复/撤销及窄屏通过。身份为隔离夹具，未调用模型，**不是 Snow 的对话浏览器任务通过**。临时精确域名 DNS 已恢复，测试隧道已退出；不放宽 SSRF 规则。
- 源码构建、类型、lint 与候选首轮四项 CI 通过；新增修复后的检查以最终提交为准。证据保存在本机私有 `.local` 和临时验收目录，含租户内容的截图、认证配置不进公有仓库。

## 真实浏览器验收发现与临时权限

- 用户明确批准仅 Dev 的现有平台管理员临时获得 Snow 工作区 admin；Snow 本人仍为 member。通过正式发布/分配服务创建验收员工版本 `bfa5a801-a132-4640-9ad4-f1be56417db4`，配置 Snow 单一策略、精确站点浏览器 grant。Server gates 临时开启不是其他租户授权；只有 Snow 有 enabled 策略。前后值及回退记录保存在私有目录。
- 候选 `038f946013ee8c9ed2f2da6e1b3084195313313d` 上，由 Snow 网页真实提交浏览器 Run `17fbc51a-456c-469e-962b-f835e66a9b46`。刷新后在网页批准单一导航，实际取得 URL、时间及截图，工件 `5c55f84b-7511-40c6-b9ab-d16ac2ec2b51`。**最终任务失败，不计整链通过**：日志明确为数据库死锁→Harness 中断→不完整用量→重试被 `MODEL_TOKEN_USAGE_UNKNOWN` 拦截，不是已证实的 Codex 官方额度不足。
- 首轮中断时保留原未知账本，未伪造成 0 或完整用量；随后收到用户明确批准并完成下节的预算例外。首轮浏览器已物理关闭，临时站点 grant 已撤销，精确域名 DNS 例外已验证恢复，测试隧道已退出。
- M5 最后心跳在 2026-09-21 09:01:33（上海时间），随后 SSH 超时。本轮尚未修改其授权目录、启用沙箱或写入测试文件；不能以先前固定 ARM VM 探测代替真实文件任务。
- 新增修复：真实 PG 三套控制/锁顺序/本地浏览器回归 **46 通过**；真实 Chromium 工作台与输入隔离 **30 通过**；lint/typecheck/DSH 校验通过。最终候选 SHA、Dev 部署、原研究 Run 复验及临时配置撤销以收尾记录为准。

## 2026-09-21 第二轮结果与恢复核验

- **部署与回归身份**：Dev Web/Worker 的实际代码为 `692aa3aad263cdb4497729b7b7be941f6fca5b17`，独立目录 `allrice-dev-releases/met147-ux01c-recovery-20260921`。公网 ready 返回同一 SHA。该代码 [CI 35550455391](https://github.com/semiok/allrice/actions/runs/35550455391) 四项全部通过。后续验收记录提交仅改本文，不冒称重新部署了文档提交 SHA。
- **实际浏览器整链通过**：Snow member 从网页提交 Run `ff704aec-004c-48eb-9968-90d115733148`，Session `3433571b-982a-43c6-a172-75997934d1ed`。Codex 订阅任务在网页刷新后经精确导航审批，实际访问自有合成 HTTPS 页面，读取标题/日期，生成 URL/时间/截图工件并 succeeded。工件 `bf4889cc-0aa1-4581-8c09-ba0c7d2829f2`；刷新后可再次打开真实截图，像素已加载。不是用模型回复中的“验收通过”代替检查。
- 本次浏览器任务输入 22,801、输出 1,174、总量 23,975、已记录缓存输入 10,752；`usageComplete=true`、`cacheUsageKnown=false`，因此不把缓存字段冒称完整统计。浏览器最终 `closed` 且存在实际停止回执。
- **旧/新报告恢复通过**：同一真实研究 Run `b37598ef` 在最终代码上重新检查展示、聊天下载链接、下载 SHA 和刷新，不重发研究任务。历史 `49fe3f09` 的 2,016 字原回复仍在，右栏明确为消息预览、非已发布工件，无假下载按钮、无旧预算误警告，不修改旧消息。
- **报告内容质量缺陷，独立记录**：人工抽检发现研究工件把 Circle 的 7.33 亿美元储备收入写成 FY2025 全年，官方材料明确该数字属于 **Q4 2025**。见 [Circle 官方财报第四季度段落](https://www.circle.com/pressroom/circle-reports-fourth-quarter-and-full-fiscal-year-2025-financial-results)。交付/展示/下载链路通过不表示财务事实全部正确；保留原版本与证据，未私改工件或用脚本替模型改答案。后续可借既有修订反馈流程复核，不扩大为本 PR 的金融研究内核重写。
- **用户批准的未知用量处理**：通过正式管理员 review 服务，仅为中断 Run `17fbc51a` 的 route decision `6afbd49f-6ac2-4da5-bc73-6a6ec5cc2348` 保留 1,000,000 Token 内部预算。原账本仍 `usage_complete=false`，review 的原始账本快照与当前账本一致；这不是实际用量或新增费用，不重放未知任务。例外审计保留，不随临时权限回退删除。
- **临时配置全部撤回**：两轮授权均有前后记录；最后 Snow 仍 member、临时管理员 membership inactive、临时 500 万用户配额已删除回到原默认 200 万、员工 assignment 恢复、策略 disabled/plan_only、浏览器 grant 撤销、两项临时服务开关回到原值。浏览器已关闭，第二轮精确测试域名 DNS 例外恢复并验证，测试服务与其独有隧道进程正常退出。原会话版本/时间也已恢复，保留新验收记录及真实用量。
- **发布边界**：修复代码保留在 Dev；main 未合并、Prod 配置哈希及进程 PID 未变。恢复原月限额不清零真实累计：Snow 本月已记录用量超过 200 万，因此新聊天仍会触发内部月限额，不能把本次临时提额解释成永久开放。

剩余完整门禁：Snow 网页审批→M5 文件/命令→交付及相应真实失败路径；报告事实口径复核/修订另有明确记录。M5 仍无新心跳，不能用固定 ARM VM 探测或隔离回归替代。整单保持进行中，PR 保持草稿；不合 main、不动 Prod。
