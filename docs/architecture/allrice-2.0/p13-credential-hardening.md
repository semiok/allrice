# P13 / B4：Keychain 非交互排查与凭证一致性候选

2026-09-09。基线 `642d8f335fd66bc0c9ac260e0e5d626c8ad04cf4`，候选分支 `codex/b4-keychain-diagnostics`。本补丁仍属于 B4，不进入 B5。凌晨非交互候选之后，上午已补 `0.4.0-dev.2` 双架构 GUI 隔离实测，见下文；该证据不自动表示已合并、部署或替换下载包，实际发布记录以执行总表为准。

## 证据与不能下的结论

9 月 8 日最终 Intel / M5 原生菜单和配对测试通过，但新配对均使用私有文件回退。之前对随机合成设备的 Keychain 复现返回 security 退出码 36，并报告 `User interaction is not allowed.`。这只能证明当时的启动会话不允许交互，不能推断解锁后从 Finder 正常启动一定成功或失败，也不能把文件回退说成 Keychain 成功。

Apple 将 [errSecInteractionNotAllowed](https://developer.apple.com/documentation/security/errsecinteractionnotallowed) 定义为不允许与 Security Server 交互。本补丁不解锁钥匙串、不修改 ACL/TCC、不获取个人密码、不通过设置 Always Allow 或更换默认钥匙串来让测试通过。

## 本次有界修正

- 所有 `security` 调用设置 5 秒 timeout、64 KiB 输出上限，等待实际子进程结束后再返回；超时只终止本次创建的 `security` 子进程，不触碰系统钥匙串服务。不是只用 Promise.race 放弃等待、留下迟到写入者。
- 只对已核对的退出码与文本组合分类：交互不允许、条目不存在、超时、不可用。stderr、argv、token 和原异常 cause 不进入 UI、日志或诊断。输出超限不能误报为超时。UI 解释是最近一次访问/保存记录，不宣称每次打开状态窗都会探测或修复 Keychain。
- CLI 只有配置文件确实不存在才进入首次配对；已有配置的解析错误、凭证缺失/暂不可读不再消耗新的配对码、覆盖身份。
- 新配对使用配置文件旁的独立 `0700` 凭证目录，每个设备一份 `0600` 来源记录。Keychain 来源记录不保存明文 token，并校验已保存 token 摘要；文件来源只读取本设备记录，不让之后可读的陈旧 Keychain 项覆盖它。Keychain 来源暂不可用时拒绝启动，不复活陈旧 legacy token。
- 已有设备凭证不隐式轮换或自动切换存储源。新设备的写入不能在配置提交失败时污染旧设备。显式迁移仍是待设计/验收事项，不借此引入跨 Keychain 与文件系统的假事务回滚。
- 旧的 `config.json.token` 只读兼容限于落盘配置中的同一 deviceId。读取不触发迁移、修复权限、删除或写入。非本人、非 `0600`、软链接/硬链接、非普通文件或过大文件均拒绝使用；不能先读取不安全文件，再仅在 UI 上警告。
- 新文件先在私有目录内以独占临时文件写入、同步，再原子替换。拒绝不安全的原目标，不自动 chmod 父目录，不以失败清理删除旧凭证或他人替换的文件。这些检查不宣称防御拥有同 UID 任意进程控制权的攻击者。
- 远端确认撤销后，本机清理的成功/部分失败单独返回；不能因 Keychain 暂不可删而用已失效 token 重试远端撤销，也不能把部分清理说成所有本机凭证均已删除。清理失败保留待核实提示，不绕过系统权限。

## 验证边界

不依赖桌面的验证包含：隔离文件与 mock Keychain 的来源/身份/失败路径；使用真实 Node 测试子进程验证 5 秒超时和输出超限确实结束进程；真实桌面 Core 子进程读取合成设备文件、状态/诊断输出及安全退出；Intel / ARM Swift 类型检查。这些都不等于真实 Keychain 成功保存。

所有测试只使用临时目录、合成 token 与自有子进程；系统 `security` 不作为模拟测试的执行目标。模拟端口只存在于测试文件，不添加生产环境 override。

### 凌晨非交互候选验收结果（历史记录）

- 当次完整 Bridge 测试：**305 通过 / 42 跳过 / 0 失败**，28 个测试文件通过、3 个门控文件跳过；42 项需要额外沙箱、网络或平台条件，不计作通过。
- `pnpm --filter @allrice/rice-bridge typecheck`、`build`、13 个变更 TypeScript 文件的 ESLint，以及格式与 diff 检查通过；构建仅生成本地 Core 产物，没有打包或替换 App。
- `xcrun swiftc -typecheck -target x86_64-apple-macosx13.0` 与 `arm64-apple-macosx13.0` 对最终宿主源码均通过；交叉类型检查不是 ARM 实机或原生菜单验收。
- 新增桌面协议端口测试验证部分清理的两种结果，以及显式新配对后旧清理提示仍保留、新设备正常运行；不是实际 macOS 部分清理 UI 已通过。
- 最终报告位于候选工作树 `.local/keychain-candidate-20260909/bridge-final.json`。接口/夹具同步期间一次全 Bridge 运行有 8 项旧断言失败；修正后完整回归先为 302/42，再补桌面状态测试为 305/42。初次失败输出保留，不声称首次全绿。

## 实机验收口径与保留门禁

1. 普通 Finder 无参数启动会读取日常默认配置，不能把“App 位于临时目录”误当配置隔离。本轮使用 macOS LaunchServices（`open`）从已解锁 GUI 会话启动原字节 App，显式 `--acceptance` 仅传独立配置路径与 ledger=0，不注入设备 token，不改 HOME 或全局 launchd 环境。该结论是 GUI 隔离启动，不冒称默认 Finder 无参数/真实身份已验。
2. 只使用随机合成账号和隔离配置；确认实际 Keychain 写入、读取、正常退出及再次打开均成功。检查文件中没有回退 token，诊断中没有秘密。
3. 分别核对 Intel 和 M5；其中一台通过不能替代另一台。旧受限会话失败记录保留；本轮不修改系统设置制造钥匙串拒绝或解锁，用户主动拒绝 macOS 授权弹窗的路径仍单列未测。
4. 验证已有私有文件配对仍能使用；不自动迁移、不强迫重新配对。显式旧凭证迁移尚未实现时必须保留未验标记。
5. 验证撤销的远端结果与本地清理结果分别准确，失败不删除日志或宣称成功。
6. 固定候选版本通过后才提交发布判断。最终 main 的包须重建、复验并绑定其来源和字节摘要后才能更新 Dev 下载；不拿当前已发布旧 ZIP 或前置候选包当最终包实测证据。

### 2026-09-09 双架构候选实测

包生产源 `060629395082c87bf05fd6835073b9313fa57340`，源归档 SHA-256 `72aeccc5a7a01bbfa2bfc8b1ece8b64348674ae5a3bd30ce3a5a165500e2228c`。两个 `0.4.0-dev.2` ZIP 实际解压后运行，App/SEA 字节前后一致：

- Intel ZIP：`b28ff70b72d7dec3f41cfcf8b73d446d1b11a41785eac58659bece9b0d1cf1d6`。
- ARM ZIP：`458d51f05e9cc7241390a0f546911b91165dd40c62338e4f6f6460b174549468`。
- 两台各三组，共 **6/6**：新配对实际写 Keychain、原生退出/重开后认证读取、原生撤销及精确随机条目不存在；旧 `0600` token 文件直接兼容，重开不迁移、不重新配对；合成文件在已认证撤销 ACK 前注入真实 `0400` 清理故障，界面准确显示旧凭证未清完，远端只撤销一次，之后恢复/清理本轮文件。
- Fresh 每台只配对一次、只撤销一次；严格 loopback fixture 校验所有已配对请求的 Bearer 与本轮随机 token 相等。Keychain 来源记录仅含摘要，无回退 token；重开计数基线取自旧进程已退出之后。各组未预期请求与清理错误均为 0，全部本轮日志检查未见合成 token。
- 故障组证明真实文件清理失败，不冒称 macOS Keychain 授权拒绝已测。界面证据是实际 AX 与 AppKit view 渲染，不冒称整个 WindowServer 截屏。独立 HTTP fixture 不代表生产 PostgreSQL 新配对测试。
- 额外修复同一 Core 内 A 部分清理、B 新配对并完整撤销后，A 的提醒仍保留；每次响应的 `cleanupComplete` 仍只描述当次设备。新增回归后 Bridge **306 通过 / 42 跳过**；此前 305/42 属凌晨候选。
- M5 日常 App 测前正常菜单退出、测试后正常重新打开；日常配置及旧 App 校验值未变。所有实机测试使用随机合成身份，不修改日常配对、目录或 Keychain ACL/TCC。

私有证据：候选工作树 `.local/keychain-native.1BkzXz`。Intel 三份结果为 `allrice-b4-gui-keychain-Ct78XW` / `zwWrQw` / `g6SPAh` 下的 `result.json`；M5 三份结果已复制为 `m5-fresh.json` / `m5-legacy.json` / `m5-partial.json`。此前 `a73e122` 的 Intel 首次通过报告单独保留，不混为最终候选版本。

### 全仓回归与测试启动门槛修正

首次全仓运行保留在 `.local/keychain-native.1BkzXz/full-tests.json`：1465 通过、3 失败、362 跳过。三个失败都发生在桌面源码 Core 首次启动的约 3 秒夹具等待窗；同一原文件隔离运行 12 通过 / 1 跳过，失败项仅使用私有文件中的历史原因字段，不访问实际 Keychain。

仅将测试源码启动等待分离为 10 秒，启动后的原行为断言仍为 3 秒；涉及已有 5 秒 ledger flush 的停止请求独立为 7 秒。子进程异常提前失败，退出等管道排空，诊断只含安全枚举/计数。产品 Keychain 超时和原业务断言不变；新增 3 项等待器边界测试，最终定向 15 通过 / 1 跳过。

修正后完整全仓 **1471 通过 / 362 跳过 / 0 失败**，188 文件通过、24 门控文件跳过，报告为 `.local/keychain-native.1BkzXz/full-tests-after.json`。全仓 build、lint、typecheck、DSH 校验、格式及跨应用集成类型检查通过；跳过项不计作通过，PR CI 与最终 main 实机/Dev 验收另行记录。

## 已知保留限制与回滚

撤销成功响应中的 `serverRevoked`、`cleanupComplete`、`configDeleted` 分别表达远端撤销、本机完整清理和配置删除结果，`ok: true` 不等于本机全部清理完成。桌面 `credentialCleanupPending` 目前只保留在当前 Core 进程内，重启不恢复，也没有新增清理重试入口；重新配对后提示明确指向此前操作的旧凭证，不误称新设备已经撤销。原生 UI 的文件部分清理失败已在两架构实测；Keychain 用户拒绝删除的系统交互未包含在这项结论中。

当前仍调用 `security add-generic-password -w token`，token 在该子进程 argv 中的既有风险没有被这次错误脱敏解决。原生 Security API 替换需独立审查及双架构实测，不用交互式 shell 拼接或变更系统权限临时绕过。

Keychain 的双架构 GUI 隔离成功已补实测，但默认 Finder 无参数真实身份启动、显式旧凭证转存、正式签名/公证/升级回退仍未签字。新设备来源记录是新的本地格式：不要把它删除后强制退回旧读取器；旧二进制不理解新的文件来源记录。候选尚未发布时可保留源码而继续运行现有版本；未来发布后的回滚必须保留新格式读取兼容，不能让旧 fallback 再次成为权威。

唯一执行顺序仍以 Linear 执行总表为准。本文件仅记录 B4 修正范围和验证约束。
