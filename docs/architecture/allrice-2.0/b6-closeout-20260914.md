# B6 收尾与工单校正｜2026-09-14

本轮先完成既有 MET，不启动新的三栏常驻工作台。唯一执行总表已校正并补实测至 v1.36，顺序为 **MET-139 → MET-140 → MET-142**；MET-141 配合固定候选，正式 Bridge 签名独立记录。

## 工单不是按红色图标直接关闭

Linear 红色关系徽标表示阻塞/被阻塞的依赖数量，不等于一个额外未命名 Bug。

| 工单              | 校正后的事实与剩余项                                                                                                              |
| ----------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| MET-131           | P18 实现及对应验收已经交付，校正为 Done，关联已合并 PR #51。                                                                      |
| MET-108           | 子工单尚未完成；撤销合并 PR 引起的提前关闭，恢复 In Progress。Boost/Teamwork 后续阶段不冒充本批实现。                             |
| MET-139           | PR #64 已合并不等于真实助手验收完成。本轮修复未知用量后的重复模型请求，补价格冻结、全树计量和 Worker 配额核对。                   |
| MET-140           | PR #65 已合并；本轮补齐发送/附件/问题回答/取消的会话隔离与断网恢复，真实助手联合验收仍依赖 139。                                  |
| MET-142           | PR #66 已合并；需要在固定最终候选上完成联合矩阵，不能用脚本存在或预检成功替代执行。                                               |
| MET-138 / MET-143 | 138 保留可信安装/升级的技术范围；143 是单列的 Apple Developer、Developer ID、公证及双架构正式可信包待办。开发包不是正式签名验收。 |

真实依赖继续保留：139 阻塞 140；140、141、138 是 142 的验收前置；143 阻塞 138 的正式可信分发验收。非签名场景可以先做，不据此声称 GA 已通过。

## 源码与环境

- 隔离工作树：`/Users/a123/allrice-b6-closeout-20260914`。
- 分支：`codex/b6-closeout-139-140-142`；基线：`5d246ba7a85b5640121cbcc8dc1336f52d0a74df`。
- 原 `allrice`、`allrice-dev` 脏工作树保持不动；不在服务中的 release tree 开发。
- `pnpm install --frozen-lockfile --offline` 与 `pnpm dsh:verify` 通过；固定 SDK，不修改 node_modules，不批准新的依赖安装脚本。
- 本轮没有部署 Dev/Prod，没有打开真实租户的助手或其他高权限开关。
- PG 仅使用既有专用 `allrice_b2` 库内随机隔离 schema；Chrome 仅使用私有测试 profile/loopback 页面。测试结束清理其拥有的 schema、进程和临时工作文件，保留不含密钥的验收收据。

## MET-139：已复现的运行缺陷

固定 DSH 的原生 Gemini provider 在 HTTP 503、没有可信用量时，仍可被上层循环再次调用。首轮合成 HTTP / 真 DSH / 真 PostgreSQL 回归明确发现实际 3 次请求，不应仅凭单次 `adapter.execute` 声称不会重复调用。

修复后，只有持久结算确认且输入/输出两个维度都有可信用量时才释放原生 admission。未知用量、结算 RPC 失败或负 ACK 保留不可重放标记。助手 503 路径仅发出 1 次 HTTP 请求；未启用助手的普通聊天保留原有有限重试。

初次修复相关 6 个文件 43/43 通过。后续价格接线后的最终复验应另行记录，不能将此数字外推到新的全部代码。

### 费用语义

旧通用价格函数缺价返回 0，且不能用缓存未知占位 0 推导免费。助手不复用这一默认行为：冻结精确服务端路由及版本化价格，再用持久的真实调用 Token 回执计算整树估算。

当前仅证明 Token 总量时，所有输入按该价带最高输入单价计上界，输出包含思考 Token。`costBasis=conservative_upper_bound` 不等于真实账单，`actualCostKnown=false`，`cacheUsageKnown=false`。缺价格、超价带、缺用量或缺调用回执保持拒绝/unknown，不能将历史 NULL 改为 0 或事后补造已知费用。订阅费用不能套用 API 单价。完整单项测试与 Worker 账务接线的复验结果待固定候选补录。

独立审核发现旧月账无币种列的混算风险，已在 Worker/controller 拒绝非显式 USD；通用回执保留 currency 不等于支持多币种旧账。启用前仍须人工核对既有普通聊天价格的 USD 口径，不改历史行。

价格合同、真实隔离 PG、Worker 预检与账务/后续配额定向 **55/55 通过**（17+11+13+14）；其中 Worker 成功结果来自合成 adapter，不是 Google。定价 native 全组前一轮 **55/55 通过**，使用真 DSH/Google SDK 和合成 loopback HTTP；随后补非 USD 拒绝的定向 **19/19 通过**，两组有重叠，不累加当作独立用例。缺价、到期不覆盖任务、错误路由、图片和非 USD 均在调用前已知零拒绝。

第一轮整仓普通测试保留 **2102 通过 / 1 失败 / 797 跳过**：失败是报价到期早于任务截止时间的负例。最终源码的单项独立复跑通过，但不能凭此认定整仓通过或证明是并发编辑导致；保留 `.local/b6-closeout-tests-first.json`，最后冻结后重新跑整仓。

产品与测试源码停笔后的整仓普通复跑 **2139 通过 / 0 失败 / 801 条件跳过**。收据 `.local/b6-closeout-tests-final.json`，SHA-256 `eacb9a7e0d66463600f74d114ea9d34881bcefbd4039159c3301f8421875beb3`；测试时仅继续更新说明文档。条件跳过需要各自环境单独验收，不计作本轮通过。

随后按 CI 的 B6 专门列表开启真实隔离 PostgreSQL / 原生 DSH 整组复验，**22 文件、281 通过 / 0 失败 / 0 跳过**。包含权限、恢复、输出、两阶段准入、新价格回执、实际 Worker 预检/账务及原生助手；模型端点为合成 loopback，不是 Google。收据 `.local/b6-closeout-pg-final.json`，SHA-256 `bf81eaea37b22ffd4825b921e145bfe36224b4d7b92000fbd86cf47a5e5518dd`。全仓 typecheck、ESLint、跨应用 integration TypeScript 和 16 个 P27 scripts 全传递类型检查通过，`pnpm dsh:verify` 仍为固定 `dsh-0.1.1-rc.2-b150a55`。

## MET-140：前端修复与两层实测

本轮仅修复已有助手/聊天界面，没有新增三栏设计。

- Session 代次和请求 owner 双重约束，A→B→A 也不能使旧请求重新有效。
- 迟到的发送、上传、工作区附件绑定、Ask User、取消结果不会覆盖其他会话的草稿、附件、busy、错误或 SSE；离开页面不取消已提交的后台任务。
- 卸载后的停止 ACK 不再刷新另一个会话；网络错误被捕获。
- 长断网耗尽有界重试后，恢复联网重新读取权威助手详情/汇总，不重放取消，也不伪造停止确认。

真实 Chrome + React StrictMode、合成 loopback HTTP **14/14 通过**；同一夹具内存加载 main 原文件，**4 项明确失败**，构成修复前反证。ChatFlow/P26 单元/API 定向 **153/153 通过**，Web typecheck、ESLint、Prettier 通过。

主开发随后执行 `pnpm --filter @allrice/web... build` 和 `pnpm exec tsx scripts/acceptance/ui/p26-assistant-workbench.ts`。真实 Chrome + built Next + 随机 PostgreSQL schema 联验通过，桌面/390px、持久历史、停止请求与 ACK 区分、跨 Session SSE、普通 Codex 入队不启用助手、flag OFF 后历史/取消可用均有断言，Page/Console/HTTP 错误为 0。它是合成持久历史测试，没有 Worker 或模型调用，不替代真实助手联验。

本次开发中复验收据：`/tmp/allrice-p26-ui-cqrmA4/evidence/checks.json`，SHA-256 `3240e46f43e6b550f4b4ff13108ffe4b0574968ef3397dd64ba429b3d8d0c25e`。主开发已查看桌面截图。由于运行时还有其他模块的工作树改动，这是分项开发证据，不是干净最终候选的 P27/RC 通过收据。

## MET-142：验收脚本与凭据隔离

P27 明确区分默认 Codex 和显式 `--provider=gemini`，真实执行必须同时绑定完整 SHA 与 provider 授权；不能从失败的 Codex 静默切换 Gemini。预检仍不调用模型、不打开数据库、不读取凭据。

Gemini 验收必须使用新建、私有、空的 DSH platform home：固定 `credentials-local` 在启动时会加载其 home，即使模型选择 Gemini，也不能把它指向现有 Codex 凭据库。仅正常生产 resolver 可读取显式给定、经过元数据检查的 Dev Gemini 文件；验收脚本不复制/打印密钥，不继承其他 provider 密钥环境。平台目录创建与环境设置也纳入 owned-resource 清理。

P27 最终脚本的纯预检、价格对账、全树返回值、凭据元数据、空 platform home 和 owned-client 测试 **105/105 通过**，隔离真实 PG fixture **2/2 通过**，脚本及传递依赖 TypeScript 零诊断。这些不构成真实 Google 调用成功。

显式 Gemini fixture 绑定经核对的公开价格事实、随机冻结路由、到期日 2026-10-01 和 11 美分 Token 价带上界；逐笔对账 admission、不可变费用回执、根 Token 账本及 adapter 返回值。它只验证有价 native 助手子集，不替代完整 Worker 连续配额、四条业务线、供应商账单或正式分发矩阵；需逐项留证后再关闭相关工单。

P28 校验器已纳入迁移 0096 的精确哈希、旧版本兼容及回退保留冻结价格/费用回执的必需断言，新增负例后 **27/27 通过**。draft manifest 仍未批准，没有虚构最终 SHA、签名或矩阵执行收据。

## 干净候选实测｜19:22～19:25

代码分为三个本地提交：139 `1c3a921`、140 `d9e7a29`、142 `7161caa`。完整候选 `7161caad6e74ca6410ddc2539fa36ce85a07e782`，工作树干净，整仓 `pnpm build` 通过。没有推送、创建新 PR、合并 main 或部署。之后只追加本节实测文档，不借本节文档提交声称重新执行过模型。

### P26：同候选真实页面复验通过

- 真 Chrome + built Next + 随机 PG，BUILD_ID `Lp3WuGAWIBUwChbs2TQfo`，合成持久助手历史；没有 Worker/模型，也没有 API mock。
- 桌面/390px、刷新、flag OFF、停止请求不冒充退出、旧 SSE 不重建、下一任务不改当前冻结模式均通过。Page/Console/HTTP 错误为零；切换时主动取消的请求保留 `ERR_ABORTED` 记录，不改写成零网络事件。
- 收据 `/tmp/allrice-p26-ui-Chng8r/evidence/checks.json`，SHA-256 `bab54a19b8c5657af2808958b64b5b3d2442fd6e049d917c7c2822da20e03447`。主开发已检查桌面截图。真实 Chrome/StrictMode 的独立迟到交互回归另复跑 **14/14 通过**。

### P27：真实 Gemini 失败，不能关闭 139/140/142

- 默认 Codex 预检继续明确输出上限不受支持；显式 Gemini 预检通过。两次预检均未调用模型、未打开数据库、未读取凭据元数据。
- 19:22 开始一次 SHA/provider 双绑定的真实 Gemini 执行；正常 resolver 仅使用既有 Dev 指定绑定，新建空私有 DSH home，不复用 Codex home。不启用真实租户，不进行第二次整场请求。
- 70,122 ms 返回 `ASSISTANT_EXECUTION_UNRESOLVED`：父 unknown，两个子助手分别 completed / partial。全树已登记 8 次模型调用，确认输入 11,164、输出 503；partial 子助手仍保留输入 1,499、输出 4,000 的未知用量预留，没有当作零释放。不能从这些数值推导准确账单或证明全部模型请求正常完成。
- 脱敏收据只保留上述状态和泛化错误，未能定位该子助手最初为何缺少完整用量；目前不能声称是 503、代理、超时或价格上限导致，更不能凭合成测试绿而关闭它。下一步应先补首因的结构化、脱敏诊断及相应子助手失败回归，再进行新的独立真实验收；不能自动重放未知调用。
- 只读代码核查发现诊断丢失路径：固定 pi-ai 可返回带 failure 的错误型 finish 而不抛异常；当前 native 只收 usage，子结束概括为 `native_settled`，无 report 再合成 generic partial，根最终只抛未决账务异常。下一轮须同时覆盖 finish/throw/settlement，保留第一条绑定 call 的白名单元数据，不让次生结算错误遮住首因；这解释了证据不足，不证明本次最初就是某一种 provider 错误。
- 证据 `.local/p27-assistants-a6fc4add-8288-48f1-a0c8-ee1cd08f7a23/03.json`，SHA-256 `d5a1cc8907d0354259e421bd56b11c22bab386df354c6851d081d4e553e9ed68`。native host 1/1 退出、execute settled、随机 schema/fixture storage/连接/private temporary 全部确认清理；保留失败收据，不保留原始模型 JSONL 或密钥。

结论：记录已校正，一批真实缺陷已修复并本地分项验证，但本批没有全部完成。139 的真实助手成功/费用与完整 Worker 连续配额闭环、140 的真实模型 UI 联验、142 的最终四线/设备/兼容矩阵仍待完成；143 继续独立外部待办。红色真实依赖保留，暂不开始新三栏体验，也不批准 RC/GA。

## 继续收尾｜21:40～21:55

### 141 准备交付完成，138 非签名诊断收口

- 141 原发布清单/checker 已随 PR #63 合并，本次补充 [47 场景交接索引](p28-handoff-index.md)、材料来源/负责人和准备与最终验收边界。生产 checker 门槛未改，主开发独立复跑 **28/28**。本地补充提交 `e7f6f6a`；按本单原“准备、先供 P27 联验”范围标 Done，不再形成等待 142 最终成功的循环。最终候选/签名/真实矩阵/授权仍由对应工单交付，草稿仍正确拒绝发布。
- 138 的历史锁异常没有可复现生产失败或已证 raw fd 路径，不猜测性改锁。补齐 IPC 精确就绪、存活/持锁证据、失败句柄清理和实际 POSIX 锁丢失负控，主开发独立复跑三文件双 worker **21/21**；原相关工作负载 **6/6**、28 名称筛选未执行。历史失败仍保留为“未复现、归因不明”风险，列明 reopen 条件；不无限要求重建已销毁现场。本地提交 `0d759b5`，见 [P14 风险记录](p14-trusted-updates.md)。正式签名/公证与最终双机验证仍未通过，138/143 不关闭。

### 139 首因诊断已接通，负向回归通过

提交 `a8ee9bc`：native 按调用保存首个 finish/stream/usage/settlement 原因，64 项上限、固定码、根身份过滤、结束清理；结算次生失败不覆盖首因。`QUOTA` 按固定 SDK 常量归一化。未知 input/output hold 与 no-replay 不变。Worker 2 秒有界读取，安全 WeakMap 同时覆盖抛错和 partial 返回结果；失败/取消事件通过原 job lease 保存纯净元数据，不写错误正文/思考/密钥。

提交 `9254fec`：P27 失败快照与本 fixture 的真实 native/run/call/request-digest/价格回执关联。独立回归：原生准入 **19/19**；DSH/GoogleSDK/PG + 合成 HTTP **13/13**，包括子 B 503、该子仅一条实际 loopback HTTP 请求、首因 `finish/SERVER` 经真实 RPC 进入关联证据。后台 Worker 隔离 PG **21/21**，含未知账务、取消及事件保存失败不盖原错。各重叠套件不相加。

集中 B6 真 PG/native 合成模型回归 **301 passed / 0 failed / 0 skipped（22 文件）**；报告 `.local/b6-closeout-diagnostics-pg.json`，SHA-256 `a1913a3852a2a363aa7dc039f01322a76884dc796978ecf7bb490fd258d775b4`。P27/诊断纯测试 **139 passed / 2 PG 条件跳过**；DSH golden replay **36/36**，outcome 回归 **6/6**。全仓 typecheck/lint/format/build、P27 脚本 TypeScript 零诊断与 dsh pin 校验通过。这不是新版本的全仓普通测试计数，也不替代真实 provider。

### 9254 真 Gemini：首请求 SERVER，首因可追踪但成功未通过

- 干净完整候选 `9254fece7de28e4c40e28578a7031b859cc103be`，同 SHA 再执行 frozen-lockfile offline install 与 dsh verify，显式 Gemini 预检通过。
- `2026-09-14T13:54:24.549Z` 开始，`13:54:31.234Z` 完成清理；adapter 执行阶段 **3,089 ms**。一次 execute、一笔模型准入、未创建子助手，root 首请求得到 **finish / SERVER**。这只是固定 SDK 的分类，未取得明确 HTTP 状态/错误正文，不能据此断言 Google 而非中间代理，也不能倒推 7161 那次失败的原因。
- 新诊断与同一 call 的 admission、request digest、不可变价格 receipt 真实匹配：`dispatched=true / finished=true`，但 input/output 都未报告，`receiptUsageComplete=false / receiptCostKnown=false`。输入 2,897、输出 4,000 是未知预留，不是已确认消费或零账；没有自动整场重试，不把单次 execute/一笔准入称为供应商逐 HTTP/账单证明。
- 根在错误清理后为 canceled；外层 HandlerError 的 retryable 元数据仍为 true，不据此宣称已重放。完整 Worker 层的未知账务/quota 与新 lease root no-replay 语义需独立复核，不能把 adapter-only 试验当作该项验收。
- 收据 `.local/p27-assistants-fcf79f4d-719c-4604-955b-ce2b4d0d5a7a/03.json`，SHA-256 `88d01a387a71697105cc39fe106bf7534cf3ff708e6fac1836ea616247b5a600`。host 1/1 关闭、execute settled、schema/storage/全部本次连接/临时目录确认清理；保留新旧失败收据，没有留下原始 JSONL。
- 随后经既有 `127.0.0.1:7897` 代理对公开 Google models 端点做无凭据 GET，1.46 秒收到 403；只说明该探针链路能返回 HTTP，不能证明付费模型请求健康，也不是当前已配置密钥认证失败的证据。没有改代理配置或凭据。

下一步在 139/142 原范围补齐完整生产 Worker（真实准备/冻结/队列/路由/计价/月额度）隔离验收及成功后下一条普通任务；不复用一次 adapter 的成功票据冒充两次 Worker 调用，不重放旧 unknown。141 Done 不解除 139→140→142 与 143→138→142 的未完成项。本节所有新提交仍未 push/新 PR/合 main/部署，既有 Dev/Prod 身份不变。

## 连续任务与准入收尾｜22:22

### 139：真实跨 Run 缺口与最小修复

不是同 Run 原生重放：已证实新 Run 的 Worker 可先读到可用额度，随后另一个 Run 落下 unknown 月账，而前者的准入端没有重新检查；另有终止/过期根任务保留已派发 Token 未结预留、但月账投影尚未写入的缺口。隔离 PG 红报告保留 **3 failed / 3 passed**，没有模型调用：`.local/assistant-quota-admission-red.json`，SHA-256 `f921f8c0ca8e3847ee4f4d4580dafd743793c810b9bc9d60c7bc09b533c80f9f`。

修复使用既有 tenant advisory 锁，route outcome 先取 tenant 再取 route 行锁；准入拿到相同锁后在同 transaction 重读额度和资源状态。只读补查已派发、实际 Token 未结且根/工作租约终止或失效的记录；不清 hold、不将未知费用改零。正常 live 有界并发、仅 prepare 未 dispatch 和其他租户不被误拦。UUID 锁键大小写归一化，业务身份本身不改。

新增准入 **11/11**，连同 route unknown 与 Worker 预检 **50/50**；真实锁等待证明账务提交先于等待的准入检查，无定时 sleep 伪造竞争。报告 `.local/assistant-quota-admission-green.json`，SHA-256 `5059628a8984de48d6b4041e6994ebd80a63dba23958c362637832f9de386cbe`。已加入 CI 的真实 PG B6 命令。边界是新 Run 准入线性化；不追杀已经准入的任务，也没有暗增逐调用全局熔断。

### 142：完整 Worker fixture/driver

新增 [完整 Worker 连续任务验收](./p27-worker-smoke.md)，使用真实准备/冻结/队列/选路/执行/费用/月额度接口，首任务实际成功后才允许下一条禁用助手的普通聊天。独立两次执行授权，显式 Gemini 和完整干净 SHA，不继承线上数据库、不自动重试旧 unknown。普通聊天的旧估算器仍明确标注非助手逐调用回执、非账单保证。

fixture 真实 PG **8/8**；修复初始化失败原错误丢 cleanup proof、null proof 假绿、native 退出超时后漏停 heartbeat 三个验收清理问题。根任务最后集中复跑准入/fixture/预检/脱敏 **72/72**，其中没有模型调用。保留根任务前一轮 **71 passed / 1 failed** 事实：旧测试仍期望 null cleanup 为 true，与修正后的未知不能算已清理语义冲突；删除该旧相反期望、保留 null 拒绝断言后复跑通过，没有改生产安全门槛来迎合测试。P27 脚本及传递依赖 TypeScript 零诊断，全仓 typecheck 与定向 lint/format 通过。

目前尚未将新完整 Worker driver 的准备测试记成真实 provider 成功；后续必须在干净候选独立执行并保留收据。本节也不改变线上环境或工单尚缺的最终验收事实。

## a71d 固定候选与 Worker 接线缺陷｜22:35

本地提交 `9a4f081`（准入）和 `a71d0e502af92c3ccbeea3922e85e38d1a321d40`（完整 Worker 验收）。同候选工作树干净，frozen-lockfile offline install、DSH pin、完整构建、全仓 lint 与无模型预检通过。

- 普通测试 **2217 passed / 0 failed / 828 条件跳过（328 文件）**；`.local/b6-closeout-a71d-unit.json`，SHA-256 `3501d389ce3de1a38533fafb11aa62b7774710272d2ed3844c8adb70396f3d2e`。
- B6 真 PG/native 合成模型 **312 passed / 0 failed / 0 skipped（23 文件）**；`.local/b6-closeout-a71d-pg.json`，SHA-256 `060d842fb647758d739366896a66286a143d754d267bd9f5fc13702b1c386e3a`。
- `2026-09-14T14:29:19.284Z` 开始一次独立授权的完整 Worker 尝试，`14:29:23.905Z` 完成清理。首任务在 `assistant_worker_execute` 失败，原报告仅保留 `UnknownError`；admission、价格 receipt、usage 都为空，route 保存 failed/unknown。没有准备或执行第二条，不能把这次认作 Google 模型已调用成功，也不能归因于 provider。
- 收据 `.local/p27-worker-de6606c3-6e6e-4ce7-a1b5-421fef60edfd/result.json`，SHA-256 `fbd1722ac745d5766e31feaf44f7fed22896c050b8a541d89a4ac0bef08fbe4f`；失败快照与 job 失败终态确认、heartbeat 停止、host 1/1 停止、execute settle、两个连接池/schema/storage/本次临时目录清理全部确认。工作树仍干净。

随后两个独立零 provider 的真实 PG 核查复现同一阶段的确定接线缺陷：生产 `workflowLease` 比助手身份多出 `leaseMs`，controller 用对象展开将其带进 `pricing.freeze` 的严格 worker schema，触发 `ZodError / unrecognized_keys / leaseMs`；实际创建 runtime/assistant root 后，模型准入与价格快照仍为零。仅投影合法身份字段即可通过同根 bind/freeze。这解释了为什么旧 adapter-only smoke 绕开了问题：其 worker 没有 `leaseMs`。原尝试未保留 Zod 细节，不能事后改写旧收据；新回归单独记录。

修复只投影允许的 worker 身份，保留实际 native generation 及显式提供的 fence，不放宽 pricing.strict；增加真实 Worker 传入完整 lease 的回归和完整 prepare/start/conversation bind 的独立 PG 验证。诊断补充固定 `ZodError` 等类型、受限 issue 分类及唯一白名单内部键 `leaseMs`，仍不保留 input、任意属性名或 schema 错误正文。

另已只读核查固定 SDK：`SERVER` 来自 DSH 对错误文本的数字模式分类，而非结构化 HTTP 证明；Google ApiError 原有 status 在 pi-ai 字符串化处丢失，且 status 也可能来自流内 error.code。不能从 9254 分类反推 HTTP 503、Google 服务故障或代理故障。本轮不修改第三方 node_modules，也不虚构该缺失字段。

修复后主开发独立集中复跑实际 PG Worker 预检/生产测试、完整 Worker fixture、预检和脱敏诊断：**97 passed / 0 failed / 0 skipped（5 文件）**。报告 `.local/b6-closeout-lease-projection.json`，SHA-256 `e9b34c4b482a023af47279c9edda3e82d17dc66a7b55b0380640c9cc034b9c09`。Worker 构建与改动代码 lint 通过。上面的 a71d 全仓计数只属于旧候选，不冒称这次新修复的全仓执行；这轮 97 项没有真实 provider 调用，仍需新干净候选的完整 Worker 实测。

## a561 全 Worker 再验与证据关联缺口｜22:45

修复冻结于 `a5615812610ea158949959adfb66ccf39c93ea7d`；同 SHA 工作树干净、离线锁文件安装、DSH pin 和无模型预检通过，P27 脚本 TypeScript 零诊断。

`2026-09-14T14:44:09.478Z`～`14:44:32.724Z` 的新隔离 Worker 试验越过初始化和严格价格冻结，实际登记一笔模型调用，`dispatched=true / finished=true`。随后以 `HandlerError / DSH_UNKNOWN` 失败；未得到输入/输出用量，逐笔 receipt 与 route 均正确保留 unknown，第二条任务没有准备/执行。失败终态、host 1/1 退出、execute settled、heartbeat 停止、schema/storage/pools/private temporary 清理均确认。收据 `.local/p27-worker-be7abd75-0b87-4984-9750-3cf7eba5f6c3/result.json`，SHA-256 `4e9a4d18b160fed7efe96a178a1203639d42429fbac2c23483f01d0f37c79f7b`。

本次 `nativeDiagnostics.status=identity_mismatch` 不能当作确有跨会话混淆：只读检查已证完整 Worker smoke 漏传相关函数必需的 `snapshotDigest`，真实 receipt 与 undefined 必然不等。不是松开身份断言的理由；应读取该 root 的真实冻结快照并核验后关联。成功验证分支的 `pricing.summarize` 也曾 spread `workflowLease`，带入多余 `leaseMs`；需同样投影合法身份。这两项属于验收脚本接线缺口，不能逆向改写本次已缺失的首因，也不证明此次 `DSH_UNKNOWN` 的 provider/网络原因。保留原失败，补离线正负例再冻结候选。

上述两处验收接线现已修复，仍校验实际 root/org/workspace/owner、冻结快照 schema/hash/connection/catalog/tariff，以及 call/native session/request/receipt 身份。summary 仅投影正确 worker 身份和数据库 generation/fence。新增真实 PG 合成桥观测覆盖未知/已确认用量、旧漏参反例、错价格/调用/会话/root/lease 和清理证明；没有调用 native/provider。主开发独立集中复跑 **110 passed / 0 failed / 0 skipped（7 文件）**，报告 `.local/b6-closeout-worker-wire-final.json`，SHA-256 `56417936488d3977c2315161d4764055c8ff6a5023b109987cc4321184df3a5f`；P27 脚本 TypeScript 零诊断、lint/diff 检查通过。这是验收工具修正，不宣称已修复 `DSH_UNKNOWN` 或已取得真实成功。

## 最新固定候选实测与剩余门禁｜23:00

新完整候选 `7826d06d5e9595bc0d54ad3e40eacc94e08930d9` 工作树干净，同 SHA 离线锁文件安装、DSH pin 和无模型预检通过。`2026-09-14T14:54:09.097Z`～`14:56:18.979Z` 的独立完整 Worker 试验仍失败：只派发首个根模型调用，未进入两个助手分工，未准备或执行第二条普通任务。

本次首因终于与真实根、native session、call、request digest 和冻结价格 receipt 完整关联：`finish / SERVER / stopKind=error / settlementConfirmed=true`。输入/输出用量仍未知，receipt/route 费用仍 NULL；外层为 `HandlerError / DSH_UNKNOWN`。`SERVER` 是固定 SDK 分类，不是已取得准确 HTTP 5xx 或已证 Google 服务故障。不能把结算 ACK 当成已确认消费，不能复用旧 unknown 任务重试，也不能从没有产出推断免费。

收据 `.local/p27-worker-3814e0f2-f086-4022-8814-d6f7b98ecd7a/result.json`，SHA-256 `87c03f21f49e885977d94339c91eac0c4945e79dfde9bf95a85c3020fd3979ba`。失败快照和 job 终态、host 1/1 退出、execute settled、heartbeat 停止、本次 schema/storage/pools/private temporary 全部确认清理，未保留原始模型正文或思考日志。

22:58:48 另以同一正常 Dev resolver 与既有代理执行一次不生成内容的模型信息 GET，`/v1beta/models/gemini-3.8-flash` 返回 **HTTP 200（1.539 秒）**。仅记录状态，不保留正文、密钥或 headers；未修改凭据/代理、未发起生成。这证明该路径的鉴权与模型信息访问当时可用，不证明生成链路健康，也不能据此判定 SDK、代理或 provider 哪层产生前述错误。

本轮状态：141 Done（发布准备）；138 非签名诊断收口，正式包及双架构可信验证仍等待独立143；139/140本轮代码缺陷已修、110项定向回归通过，但139真实双助手成功与完整 Worker 下一任务闭环、140真实模型 UI 联验、142最终固定矩阵均未通过。需先定位生成请求失败并取得完整成功证据，不能只删除依赖图标或宣布 B6/RC/GA 完成。原失败记录全部保留。本轮修复仅本地提交，未push/新PR/合main/部署；原有脏工作树、Dev/Prod、真实租户开关均未修改，不开始新三栏优化。
