# MET-148 实现与自测记录

2026-09-20。基于 `origin/main` 完整 SHA `6349e23b84c98dd81c2417177e7e57dc6249de53`，独立分支 `codex/met-148-internal-readiness`。这是验收工具的开发记录，**不是 MET-138 / MET-142 真实产品验收报告**。

## 交付与审查

- [内部验收合同与逐断言矩阵](p27-internal-readiness.md)：保留原 47 场景，要求 45 份内部收据；161 条固定内部断言、16 条外部身份/签名断言，计费分支另外要求 API 1 条或订阅 5 条。
- 独立 manifest / receipt / report schema；原始 P28 preparation 按可信摘要原样只读组合验证，不生成假的正式收据或 Apple 成功结果。
- 强制原七份候选 artifact、迁移库存、恢复目标、默认关闭开关、双机身份、原始附件、时效和声明完整性。内部通过仍不授予 RC、部署、迁移、启用或正式分发权限。
- 自审及并行代码复核发现并修正：计费模式必须是枚举字符串，拒绝数组强制转换绕过订阅 proof；证据文件和原始 preparation 读取前，对源码 lockfile / DSH pin / SQL 文件也检查常规文件类型，避免 FIFO 阻塞。对应负例已覆盖。
- 原正式检查器、其测试和原正式草稿相对于上述基线逐字未变。原正式检查器同类计费类型问题另由 [MET-149](https://linear.app/metasnowsky/issue/MET-149) 独立修复，不将它隐藏为签名依赖或声称已修复；它是正式分发前置，不阻塞本轮内部功能链路。

## 本机自测结果

| 检查                                                                                        | 结果与边界                                                                                           |
| ------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| `pnpm exec vitest run scripts/acceptance/platform --maxWorkers=1`                           | 3 个文件、79 项通过：新增内部检查器 30 项、IO 14 项、原正式检查器 35 项                              |
| `ALLRICE_RUN_DB_INTEGRATION=0 ALLRICE_P11_NEXT_BUILD=0 pnpm exec vitest run --maxWorkers=4` | 283 个文件通过、70 个文件跳过；2519 项通过、913 项跳过；无失败。需外部环境的条件测试未执行，不算通过 |
| `pnpm dsh:verify`                                                                           | 通过；固定 DSH 分发校验，无模型调用                                                                  |
| `pnpm lint`                                                                                 | 通过；新增最终测试另跑 ESLint 也通过                                                                 |
| `pnpm format:check`                                                                         | 通过；新增文件最终单独 Prettier 检查通过                                                             |
| `pnpm typecheck`                                                                            | 全工作区通过                                                                                         |
| `pnpm build`                                                                                | 全工作区通过，包括 Web、Worker、Bridge；只生成独立工作树本地产物                                     |
| `git diff --check`                                                                          | 通过                                                                                                 |

测试材料全部为明确标注的合成夹具，包含正例是为了验证解析器行为，不属于实际产品证据。FIFO 用例只在测试临时目录创建管道；原始读取的复合验证受子进程超时保护。两个遍历全部 45 场景的测试使用显式 15 秒测试预算，避免并行构建负载下默认 5 秒误报；没有减少断言。

没有执行真实 Codex/Gemini/API 模型请求、数据库迁移、备份、双机 App 安装/更新、Dev/Prod 部署或权限修改；没有动现有脏工作树。检查器不能证明一整套相互一致的人工伪造日志真实发生，证据来源仍需独立审查，输入根目录验证期间须可信且不可变。

## 后续工作

1. 本 PR 经审查与 CI 后，按执行总表安排合并；合并或部署状态以实际记录为准。
2. MET-138 / MET-142 继续真实网页审批 → Bridge 执行 → 结果交付，以及双机恢复、排空、中断、撤权和数据保全；修复后重验，不回写历史失败为通过。
3. 内部验收齐备后推进 MET-147 工作台体验；正式签名、公证及最终签名包双机重验留给 MET-143，正式检查器类型修复由 MET-149 承接。
