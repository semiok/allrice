# B4 开工准备：云端业务与客户端体验

2026-09-08。用户已授权开工；执行总表 v1.14。B4-F 基线修正登记为 MET-127，P13 为 MET-128。
本批为 1 个独立基线修正 PR + 5 个功能 PR；分项开发、自审自测，批末统一 main/Dev 交付。
当前未完成本批、未合并或部署；下文为门禁，不是预填通过结果。

唯一排期依据为 [执行总表](https://linear.app/metasnowsky/document/allrice-20-执行总表阶段依赖pr-与验收门禁-df09b0b1e681) 第 2.1 / 4 节。
本文是对应 B4 的代码落点与验收检查单，不替代总表，也不采用早期 PR-1～16 排期。

## 本批用户目标与五个功能 PR

| 顺序 / 候选 | 用户得到什么                                                              | 复用基础与主要代码落点                                                                                                                                              | 提交 Ready 的证据                                                                                                             | 本 PR 不做                                                                               |
| ----------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| 1 / P13     | Bridge 菜单栏运行，不再长期维护终端；配对、工作区、暂停、日志与诊断可操作 | `apps/rice-bridge/src/index.ts`、`config.ts`、`sandbox-settings.ts`、journal / process manager；UI 宿主先做小型可运行验证，再固定接口                               | 新装、已有配对迁移、重复启动、断线、暂停与撤销、活跃任务退出，M/Intel 分别实测；不丢 Keychain、授权根和 Outbox                | 不另建 Agent Loop；不宣称签名公证或自动升级完成（P14/B6）                                |
| 2 / P15     | 没装 Bridge 或 Bridge 离线，也能在云端处理获准输入并交付文件              | `packages/contracts/src/runtime-v2/`、共享操作账本与 `runtime-policy.ts`；`apps/worker/src/isolation.ts`、`tool-broker/`、`packages/storage/src/`；新增云执行适配器 | 真实隔离环境完成输入→计算→持久工件→销毁→下载；跨租户/文件/网络/凭证隔离、预算竞争、整树取消、崩溃与 unknown 恢复              | 临时目录不冒充沙箱；不依赖 M5 VM 来模拟 SaaS 部署；不自动上传或搬迁本地数据              |
| 3 / P16     | 管理员配置云端 MCP 连接，并按工具授权使用                                 | `apps/worker/src/connector-broker.ts`、`packages/database/src/capabilities/connector-broker.ts`、能力冻结和审计；新增 transport / 生命周期适配                      | 一个明确授权的真实 MCP 服务任务；发现、认证、超时/取消、schema 变化、撤销、跨租户与逐工具拒绝；凭证不进入日志或模型上下文     | 连接成功不等于全部工具获准；不要求购买 Composio 或借用个人账号；本地 stdio MCP 在 P17/B5 |
| 4 / P18     | Skill 连同资源、依赖、版本一起发布和回退                                  | `packages/contracts/src/skills.ts`、`packages/database/src/platform-employees/runtime-package.ts`、已有 Skill / 员工发布和冻结快照                                  | 资源清单/hash、来源许可、依赖与缺工具状态、受控装配；旧 Run 不变，新 Run 加载指定版本；回退与篡改拒绝                         | 不另建 SkillHub；Skill 不授予执行权限；不自动加载 Hooks 或执行安装脚本                   |
| 5 / P19     | 不安装 Bridge，也能完成真实数据清洗对账并下载可重新打开的表格             | P15 + P18 + 已有 P07/P10，`skills/`、工件版本与反馈、StoragePort                                                                                                    | 两份合成业务数据→澄清→计算→版本化表格；以确定性参考结果核对行数、金额、重复/缺失/差异；下载重开、权限拒绝、容器销毁后仍可访问 | 不用模型自述代替金额核对；不把人工经验晋升 P20 或浏览器任务算入本批                      |

实施顺序采用 P13 → P15 → P16 → P18 → P19；P15/P18 的设计可独立准备，但不跳过各自前置门禁。
P19 是整批业务出口，不单独做一个只有提示词、没有真实计算和下载验证的“黄金 Skill”。

## 开工前必须收口

1. **固定基线**：main 为 `03ba9de30ef4ca0b864605a8feb5368aefcc38db`；当前 Dev 为未提交 ARM/Gemini 增量快照，不把 base SHA 写成全部上线代码的 SHA。
2. **待提交补丁登记**：Gemini `AQ.` 密钥格式、ARM Runner / Dev 下载包及测试、M5 补测发现的审批时钟问题，分别核对 diff、回归和部署状态。按用户决定留待下次提交；不能丢弃或冒充已合并。
3. **真实差异决定补丁 PR**：B4 的五个功能候选不变，基线修正不偷偷塞进菜单栏、MCP 等无关功能 PR。开工登记时明确补丁 PR 数量；若导致超过既定 5～6 个的提交节奏，先同步总表并确认，不为凑数量压成一个高风险大 PR。
4. **隔离后端选择**：P15 先确认实际 Dev/未来部署宿主、镜像与强隔离方案；证明网络、宿主文件/凭证、进程树及资源限制。M5 的本地 VM 成功不能替代云端后端验收。
5. **真实 MCP 验收对象**：先用自有受控服务跑协议及失败路径；第三方账号/数据只有明确授权后使用。没有可用授权时如实登记，不以 mock 宣称真实第三方连接已验收。
6. **客户端发布边界**：P13 的菜单栏体验不是 P14 的 Developer ID 签名、公证、更新签名和双架构回退。证书准备可提早核对，但不提前给 B6 签字。
7. **环境约束**：M5 npm 的系统 DNS 目前返回代理 Fake-IP，生产保护会拒绝；固定真实公网 IP 的测试仅用于验证 TLS/SRI 下载链路，不代表支持任意代理或已修好用户网络。

## 每片与整批的交付规则

- 每个 PR 独立说明范围、风险、自审、适用的单元/真实集成/UI/失败路径与未测项；不等待 Gemini 审核。
- 新执行能力默认关闭；测试身份、目录、MCP 数据与现有 Snow/Drink 分离；不迁移 AI-what 或读取个人 Chrome Cookie。
- 数据迁移增量兼容，复用既有 Session/Run/工件和数据库权威；可恢复输出和审计不能作为清理缓存删除。
- 批末以固定集成版本联合验证，按依赖合并 main，统一部署 Dev，再验证实际运行版本和回滚点。
- **合并、部署、启用和验收分别记录**。Prod 另需授权；完成 B4 后暂停，不自动推进 B5。

本轮 M5 补测的首次失败、修复、复测结果及保留限制见独立验收记录；本文件不预填“全部通过”。
