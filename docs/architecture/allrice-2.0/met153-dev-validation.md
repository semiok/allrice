# MET-153 Dev 验收记录（2026-09-23）

## 候选与发布边界

- PR-1：[#89](https://github.com/semiok/allrice/pull/89)，`7357ca9`。
- PR-2：[#90](https://github.com/semiok/allrice/pull/90)，堆叠在 PR-1 之上。
- 实际 Dev 候选：`6ecf81bd90ca052422616640a989958c41056983`。截至 `b39442d` 的后续提交仅补充测试/文档；下方普通聊天展示增量尚未部署，不属于这份真实 Dev 候选证据。
- 仅 Dev，未合并 main、未更新 Prod。迁移前保存 Dev PostgreSQL 备份；没有把它称为包含对象存储的全量备份。
- 兼容回退版 `731f836` 为原应用加 0103/0104 迁移清单；实际启动 Web/Worker 并验证健康，再切换候选。
- 原权限、员工版本、环境授权、Feature Flag 与 Prod 进程/配置均未改变。

## 真实入口闭环

Snow 普通成员通过正式登录和 ChatFlow 发起 `82b0d095`，不是直接插入数据库或合成 Run。

1. 管理员在租户页面实际保存“不限制”，再临时保存“1 小时”；其他字段保持原值。
2. Rice 使用真实 Codex 订阅发起 `ask_user_question`。等待期间刷新网页，提问仍可回答。
3. 数据库投影两次读取的活跃时间均为 8,156 ms，等待时间继续增加；回答“继续”后，同一个原始 Run 成功交付。
4. 最终活跃 12,460 ms，等待 10,532 ms，总历时 22,992 ms；原生模型请求尝试 2 次、工具调用 1 次、未完成调用回执 0。
5. Run 冻结的 `taskRuntimePolicy.timeoutMs=3600000`；历史模型快照的 `runLimits.timeoutMs=300000` 没有覆盖新策略。
6. 管理员在 Runtime Console 选择 Snow 及该 Session，实际看到 60 分钟、用户策略来源、活跃/等待/总历时及调用统计。
7. 恢复 Snow 原先显式配置的 30 分钟，500 万月额度及其余字段不变。默认 1 小时不静默覆盖既有管理员显式设置。

提问回答沿用现有 steer 投递机制，产生了一条已取消的投递记录 `c95ab231`；实际工作在原 Run 完成，没有把这条取消记录计为额外模型请求。

浏览器测试首次后台定位未选择 Snow，随后又因同时找到原 Run 与投递记录的两个面板而失败；只读修正选择器后通过，未重发聊天。失败日志保留，不把测试脚本定位错误记为产品执行故障。

## 自动回归证据

- `6ecf81b`：两个 PR 各四项 CI 通过；全仓 320 文件、2,857 项通过，1,096 项环境门禁跳过（不计为通过）。
- 显式 PostgreSQL 回归 82 项通过；另外执行原生 DSH 子进程与合成 HTTP 模型的暂停→继续交付、取消测试。
- 最后补充 `task-clock.integration.test.ts`：9 项通过，包括生产 maintenance → 不同 Worker claim/start/heartbeat、旧租约拒绝、重复 maintenance，以及持久审批恢复后不丢失累计时间。它不是只重新创建内存对象。
- 补充提交的 CI 曾在旧 `revoked_flag` 测试暴露时序假设：撤销后既可能直接杀进程，也可能先完成原生 drain 回执。测试改为逐个对比真实 `stopped` 成功回执与数据库 `stopped_at`，仍要求未结用量保留、无伪造结果；没有改生产权限或放宽失败断言。对应原生集成 12 项本地通过，最终 CI 以 PR 当前结果为准。
- 普通/子助手真实数据库准入与结算跨越 16/64/80；长等待、无限策略、权限撤销和输出统计用可控时钟/隔离数据库覆盖，不消耗大量真实模型请求来凑次数。

## 不扩大验收结论

本次证明了时钟与保护状态可持久化、真实入口等待/刷新/回答闭环、数据库租约接管不重置时间。**未证明强杀真实 Codex 原生进程后能自动恢复任意中断位置。** 旧进程的原生提问 RPC 需要新进程重新发起，不能重放未知副作用。

长期等待的原生进程资源释放/重建，以及完整 Worker 崩溃后的恢复闭环仍须在 MET-153 记录并验证。普通聊天独立计时展示已补代码和隔离验证（见下节），还需新候选的 Dev 验证，不能用旧候选真实短任务替代全部关单条件。

因此此次交付不自动关闭 MET-153，不自动合并 main，也不发布 Prod。

以上是 `6ecf81b` 的历史验收边界；下列接管增量分别记录后续代码和验证，不能合并成一个不存在的已验收版本。

## 2026-09-23 接管增量：普通聊天计时

- 核对 GitHub：PR #90 的原类型错误已由 `b39442d` 修复；该提交四项 CI 全绿（[35831515528](https://github.com/semiok/allrice/actions/runs/35831515528)），本地 Worker 类型检查再次通过。没有重复制造空修复提交。
- 普通回复下方新增“本轮运行时间”，展示活跃、等待、总历时、冻结时限与来源，并保留未知调用回执。无需创建助手树；旧 Run 缺少权威时钟时不补零。
- 复用已有会话交互状态轮询，一次批量读取最近最多 30 个已授权 Run（优先保留活跃 Run）。浏览器不推进计时，不新增逐消息轮询；读取失败清除本次投影并显示现有读取错误，恢复后重新读取数据库。
- 显式专用 PostgreSQL 随机 schema：时钟套件 10 项通过，新增普通 Run 的真实动作审批等待、跨租户/跨用户拒绝、旧任务无时钟以及读取不写时钟的回归。
- 前端/会话专项 5 文件 34 项通过。Chromium 工作台原有 17 项通过；新增窄屏、等待刷新、503 恢复与页面重载用例定向通过。首轮新增断言的默认等待短于产品两秒轮询，改为有界 5 秒条件等待，生产轮询没有改变；这不是新增真实 Snow/Codex 验收。
- 全工作区类型检查、跨应用 TypeScript、变更 lint/格式与差异检查通过。最新提交的全量 CI 以 PR #90 当前检查为准。
- 本增量未部署 Dev/Prod、未迁移数据库、未改变 Snow 配置、未合并 PR 或关闭工单。合并路线和剩余恢复边界见 [接管记录](met153-handoff.md)。

## 2026-09-23 接管增量：持久提问与进程恢复

- 增加 0105：保存原问题/turn/配置检查点以及分段 dispatch 状态。普通任务在静止提问等待 30 秒后释放原生进程和任务定时器；认证回答唤醒同一 Run，新租约恢复原会话并取得持久采纳证明后续接。确认过的前段用量纳入最终汇总，订阅费用仍为 N/A。
- 活跃进程丢失会保留已确认用量和答案片段，未知回执保持未知；重复 claim 不重发原任务提示词。原生已接受 prompt 后退出也能结束等待，避免永久等待一个不会再来的 idle 事件。
- 恢复重新校验成员/员工/会话权限、策略有效期、配置和 generation；错误问题回答不会触发模型调用。助手树或仍有活跃/未知动作禁止进入此提问释放路径；任意回调的自动重建不在该实现的承诺内。
- 隔离数据库、原生协议、适配器与独立 Worker 进程专项共 8 文件 71 项通过。后续补充无效答案、撤权等边界后，生产入口前置校验/订阅 handler/新等待数据库回归 3 文件 47 项通过。测试数量存在重叠，不能相加当作唯一用例总数。
- 独立 Worker 测试使用真实 queue/job-runner/adapter、原生 DSH 和合成 HTTP 模型，验证等待后强杀 Worker、新 Worker 回答并完成同 Run；活跃强杀不发起第二次模型请求。它的 handler 是精简 fixture，不等于真实 Codex 的完整业务入口验收。
- 全工作区类型检查、跨应用 TypeScript、变更 lint、格式与 DSH 分发检查通过。全仓首轮发现旧测试替身缺少新接口，已同步；桌面/原生/发布验收测试复核通过，额度超时测试单独复核通过。最终全仓与 GitHub 结果随后追加，不将最初失败隐藏为通过。
- 兼容回退版 `8631f58` 基于 `6e9c62f`，补 0105 清单和原生检查点事件词汇，已构建 Web/Worker。候选部署、回退健康及真实 Snow 重启闭环另行记录；完成前仍保持 In Progress。

### `6a1cfac` 真实重启恢复与发现的展示问题

- 0105 迁移前备份 Dev PostgreSQL，`8631f58` 在新 schema 上实际启动 Web/Worker 健康通过，再切换 `6a1cfac`；Prod PID/配置哈希、Dev 环境、额度和成员权限均未改变。
- Snow 正式入口真实 Codex Run `41a6e11c-c68e-4d7a-ba36-88412d8f17c2`：原生问题超过 30 秒后队列变为 `waiting_approval`，清空租约，原生进程清单为空。强杀 Dev Worker，launchd 启动不同 PID；页面刷新后回答原问题，attempt 2 在同一个 Run / native Session 成功交付。
- 等待跨重启期间活跃值固定 7,706 ms；最终活跃 11,847 ms、等待 291,974 ms、总历时 303,821 ms，2 次模型请求、1 次工具调用、0 条未完成回执。前段 parked、后段 completed 的 dispatch 标记均保留，费用 NULL/N/A，缓存已知性仍为 false，没有假设全量缓存回执已知。
- 验收首先发现普通计时轮询误绑 `ALLRICE_WORKBENCH_ENABLED`，因此 `6a1cfac` 不能作为普通页面展示通过的证据。随后增加独立认证计时接口，复用同一个轮询器；没有打开 Dev 工作台开关。Chromium 覆盖开关开启/关闭、窄屏、等待刷新、503 清理及恢复，两文件 33 项通过；隔离数据库计时/授权 11 项通过；前端与 CI 首轮失败用例复核 43 项通过。
- 最终证据采集脚本曾把 PostgreSQL `Result` 与普通数组作严格原型比较，以及使用不存在的用量时间列；修正只读采集后完成复核，未重发任务或改变数据。原始失败记录保留在本地证据目录。
- `6a1cfac` 全仓本地 321 文件 2,863 项通过、1,105 项环境跳过。GitHub 数据库/浏览器/Compose 三项通过，validate 的两个旧 Bridge 测试在并发负载下超时；本地分别通过。CI 改用与本地全仓一致的 `--maxWorkers=2` 限制测试竞争，不放宽产品超时或测试断言。最终新候选的 CI 和 Dev 普通页面复验另行追加。

### 显式策略投影的追加修正

`925aa4a` 四项 CI 通过（[35842068647](https://github.com/semiok/allrice/actions/runs/35842068647)），但真实 Dev 暴露另一个展示缺陷：冻结策略来源带 `scopeId`，公开展示契约只允许 `scope`/`timeoutMs`。前轮开关排查并未覆盖这个真实数据差异。数据库投影现明确移除内部 scope ID，两个读取入口的集成回归改用显式用户 30 分钟策略；没有放宽严格契约，也不向页面显示原始 Zod 诊断。

提问挂起时原生 ask 回调确实被中断，因此原始失败回执仍保留；只有已观察到检查点的受控提问中断才显示“提问已挂起，等待你的回答”，其他工具失败保持失败。真实原生 Worker 进程测试已验证这个标记，并再次通过等待/活跃进程中断两条路径。该修正的数据库/适配器/展示专项 60 项通过，新增原生标记断言的独立 Worker 两项通过。最终 Dev 候选以下方追加记录为准。
